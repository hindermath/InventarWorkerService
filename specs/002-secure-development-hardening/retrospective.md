# Autonomous Run Retrospective / Retrospektive des autonomen Laufs

## Run Identity / Laufidentität

| Field / Feld | Value / Wert |
|---|---|
| Feature and source revision / Feature und Quellrevision | `002-secure-development-hardening`; Basis-Head `dc15bc4812245e71c1a5976b8241c7aeb518d4a9` plus beabsichtigter unstaged Arbeitsbaum / base head plus intended unstaged worktree |
| Delivery evidence / Lieferevidenz | `specs/002-secure-development-hardening/tasks.md`, `docs/security/secure-development/2026-08-30-secure-development-hardening/README.md`, `specs/002-secure-development-hardening/retrospective.md`; die validierte Lineage ist in einem recoverbaren lokalen Stash vorbereitet, aus dem PR-Kandidaten ausgeschlossen und wird erst nach T117 angewendet. / the validated lineage is prepared in a recoverable local stash, excluded from the PR candidate, and applied only after T117. |
| Delivery mode / Liefermodus | `MergeAndSync`; T108–T122 bleiben beim Koordinator / T108–T122 remain coordinator-owned |
| Remote result / Remote-Ergebnis | `N/A` in T107; kein Push, PR, Provideraufruf, Merge oder Sync ausgeführt / no push, PR, provider call, merge, or sync executed |
| Interruptions and resumes / Unterbrechungen und Fortsetzungen | Der geroutete T095-Lauf war durch macOS-Sandbox-Named-Pipes blockiert; der Koordinator lieferte danach akzeptierte Host-Evidenz mit fünf hashgebundenen Coverage-Dateien. / The routed T095 run was blocked by macOS sandbox named pipes; the coordinator then supplied accepted host evidence with five hash-bound coverage files. |

## Observations / Beobachtungen

| ID | Observation / Beobachtung | Source and evidence boundary / Quell- und Evidenzgrenze | Finding boundary / Befundgrenze | Artifact kind / Artefaktart | Project exclusions / Projektausschlüsse | Generic target rule / Portable Zielregel | Occurrences / Vorkommen | Confidence / Konfidenz | Permission and evidence risk / Berechtigungs- und Evidenzrisiko | Reproducible test / Reproduzierbarer Test | Decision / Entscheidung |
|---|---|---|---|---|---|---|---:|---|---|---|---|
| AR-001 | `scripts/rename-lastenheft.sh` koppelt Rename, Staging und Commit untrennbar; die lokale Phase durfte weder stagen noch committen. / The script inseparably couples rename, staging, and commit although the local phase allowed neither. | Feature 002, T101; `scripts/rename-lastenheft.sh` und der Abschnitt `T101 berechtigungssichere Umbenennung` in `docs/security/secure-development/2026-08-30-secure-development-hardening/README.md`; nur lokaler Vor-Kandidaten-Stand, keine Post-Merge-Fakten. / Feature 002, T101; named script and tracked T101 evidence; local pre-candidate state only, with no post-merge facts. | Deterministischer Berechtigungsdefekt, keine Effizienzpräferenz. / Deterministic permission defect, not an efficiency preference. | `script requirement` | Konkreter Lastenheftname und Branch bleiben lokal. / Exact intake name and branch stay local. | Zustandsändernde Hilfsskripte müssen reine Operation und Delivery trennen, etwa durch `-NoCommit`/`--no-commit` oder einen read-only Dry-Run; fehlende Delivery-Autorität ist fail-closed. / State-changing helpers must separate the operation from delivery and fail closed without delivery authority. | 1 | High | Berechtigung hoch: sonst unautorisierter Index-/Commit-Schreibzugriff. Evidenz mittel: ein manueller Ersatz muss Hash und unveränderten Index/HEAD belegen. / Permission high: otherwise unauthorized index/commit writes. Evidence medium: a manual substitute must prove the hash and unchanged index/HEAD. | Temporäres Git-Repository; Skript mit No-Commit-Option ausführen und unveränderten Index/HEAD prüfen. / Temporary Git repository; execute the no-commit mode and verify unchanged index/HEAD. | `Promote` |
| AR-002 | Der Sandbox-Testprozess konnte MSBuild-Named-Pipes nicht binden, während derselbe koordinatoreigene Hostlauf vollständig bestand. / The sandbox test process could not bind MSBuild named pipes while the coordinator-owned host run fully passed. | Feature 002, T095; `specs/002-secure-development-hardening/quickstart.md` und akzeptierte untracked Runtime-Evidenz `.specify/runtime/autonomous-routing/d6d5e58e-0acc-404c-b5b4-4f97eba83c9f/implement-resume-t095-passed.prompt.txt`; nur der hashgebundene Host-Handoff, keine allgemeine Providerbehauptung. / Feature 002, T095; tracked quickstart plus accepted untracked runtime handoff; hash-bound host evidence only, with no general provider claim. | Deterministischer Evidenzintegritäts- und Berechtigungsdefekt, keine Effizienzpräferenz. / Deterministic evidence-integrity and permission defect, not an efficiency preference. | `evidence structure` | PostgreSQL-Port und temporäre Pfade sind keine portable Konfiguration. / PostgreSQL port and temporary paths are not portable configuration. | Ein akzeptierter Host-Gate-Handoff muss exakten Befehl, Runner, Zählerverbrauch, Ergebnisse, Artefaktpfade und Hashes tragen und eine Wiederholung bei unveränderten Eingaben verbieten. / An accepted host-gate handoff must carry command, runner, counter consumption, results, artifact paths, and hashes and prohibit reruns for unchanged inputs. | 1 | High | Berechtigung mittel: ein anderer Runner darf nicht stillschweigend Autorität übernehmen. Evidenz hoch: Wiederholung oder ungebundene Artefakte brechen Zähler- und Evidenzkausalität. / Permission medium: another runner must not silently assume authority. Evidence high: reruns or unbound artifacts break counter and evidence causality. | Fixture mit fünf Artefakten; einen Hash ändern und den read-only Rebind erwartungsgemäß ablehnen. / Fixture with five artifacts; change one hash and require read-only rebind rejection. | `Promote` |
| AR-003 | Der Statistik-Renderer erzeugt im `-WhatIf`-Modus kanonische Ausgabe, verweigert aber absichtlich Schreibzugriff in einem schmutzigen Arbeitsbaum. / The statistics renderer produces canonical `-WhatIf` output but intentionally refuses writes in a dirty worktree. | Feature 002, T098/T107; `scripts/render-project-statistics.ps1`, `docs/project-statistics.md` und untracked Ausführungsverlauf `.specify/runtime/autonomous-routing/d6d5e58e-0acc-404c-b5b4-4f97eba83c9f/implement.log.txt`; nur lokaler Writer-Vertrag. / Feature 002, T098/T107; named script, tracked ledger, and untracked execution history; local writer contract only. | Deterministischer Berechtigungsdefekt: der erlaubte Evidenz-Writer ist ohne zusätzliche Repositorymutation nicht vollständig nutzbar; keine reine Komfort- oder Effizienzpräferenz. / Deterministic permission defect: the authorised evidence writer is not fully usable without an extra repository mutation; not a pure convenience or efficiency preference. | `script requirement` | Aktuelle Statistikwerte bleiben projektspezifisch. / Current statistics values remain project-specific. | Evidence-only Phasen brauchen einen unterstützten stdout-/OutputFile-Modus, dessen Ergebnis per kontrolliertem Patch übernommen und danach mit `-CheckOnly` validiert werden kann. / Evidence-only phases need a supported stdout/output-file mode whose result can be applied by a controlled patch and then validated with `-CheckOnly`. | 1 | High | Berechtigung hoch: Commit oder Stash nur für Rendering erweitert die Autorität. Evidenz mittel: übernommene Ausgabe muss anschließend durch `-CheckOnly` gebunden werden. / Permission high: committing or stashing only for rendering expands authority. Evidence medium: applied output must then be bound by `-CheckOnly`. | Dirty temporary repository; Renderer-Ausgabe anwenden, dann `-CheckOnly` ohne Indexänderung. / Dirty temporary repository; apply renderer output, then run `-CheckOnly` without index mutation. | `Promote` |
| AR-004 | Ein hashidentisch umbenanntes Intake behält Prompts mit altem Pfad; ein aktives Authoring-Receipt kann danach nicht ehrlich „current“ bleiben. / A hash-identically renamed intake keeps prompts with the old path, so its active authoring receipt cannot honestly remain current. | Feature 002, T101–T107; `specs/002-secure-development-hardening/tasks.md` und akzeptierte untracked Stash-Metadaten `.specify/runtime/autonomous-routing/d6d5e58e-0acc-404c-b5b4-4f97eba83c9f/post-merge-lineage-stash.json`; die Lineage ist recoverbar gestasht, aus dem Kandidaten ausgeschlossen und wird erst nach T117 angewendet. / Feature 002, T101–T107; tracked tasks plus accepted untracked stash metadata; lineage is recoverably stashed, excluded from the candidate, and applied only after T117. | Deterministischer Evidenzintegritäts- und Berechtigungsdefekt, keine Effizienzpräferenz. / Deterministic evidence-integrity and permission defect, not an efficiency preference. | `runbook` | Feature-002-Namen, Hashes, Stashbezug und Reihenfolge sind lokal und werden nicht exportiert. / Feature 002 names, hashes, stash reference, and ordering stay local and are not exported. | Beim hashwahrenden Abschluss-Rename wird das alte Receipt byteidentisch archiviert, das aktive Receipt entfernt, die Serie auf den archivierten Pfad gebunden und das tatsächlich nächste Receipt validiert; fehlende alte Series-Receipt-Evidenz bleibt `N/A`. / On a hash-preserving completion rename, archive the old receipt byte-identically, remove it from the active receipt set, bind the series to the archived path, validate the actual successor receipt, and keep missing prior series-receipt evidence as `N/A`. | 1 | High | Berechtigung hoch: vorzeitiges Anwenden veröffentlicht unautorisierten Lebenszyklusstatus. Evidenz hoch: erfundene Supersession oder Promptänderung bricht Herkunft und Hash. / Permission high: early application publishes unauthorised lifecycle state. Evidence high: invented supersession or prompt edits break provenance and hash. | Legacy-Serie mit einem abgeschlossenen und einem blockierten Ziel; Rename, Archiv, Eligible-Übergang und alle Validatoren prüfen. / Legacy series with one completed and one blocked target; verify rename, archive, eligibility transition, and all validators. | `Promote` |
| AR-005 | Ein nicht isolierter Coverage-Glob hatte alte Berichte aufgenommen und 43,0 Prozent gemeldet; der isolierte T095-Satz ergab 90,1 Prozent. / A non-isolated coverage glob had included old reports and reported 43.0%; the isolated T095 set produced 90.1%. | Feature 002, zwei unabhängige Ergebnissätze; `specs/002-secure-development-hardening/checklists/autonomous-readiness.md` bindet 43,0 Prozent, `specs/002-secure-development-hardening/quickstart.md` bindet den isolierten T095-Satz mit 90,1 Prozent; keine Übertragung konkreter Raten. / Feature 002, two independent result sets; tracked readiness evidence binds 43.0%, and the quickstart binds the isolated T095 set at 90.1%; no portability claim for the exact rates. | Zweifach beobachteter Evidenzintegritätsdefekt; selbst als Effizienzpräferenz wäre die Schwelle von zwei unabhängigen Beobachtungen erfüllt. / Evidence-integrity defect observed twice; even as an efficiency preference, it meets the two-independent-observation threshold. | `evidence structure` | Konkrete Testnamen und Abdeckungswerte bleiben lokal. / Exact test names and coverage values stay local. | Abschluss-Coverage muss ein frisches, leeres, laufgebundenes Verzeichnis verwenden; ReportGenerator darf ausschließlich diesen Pfad konsumieren und alle Eingaben werden gehasht. / Final coverage must use a fresh run-bound directory; ReportGenerator may consume only that path and every input is hashed. | 2 | High | Berechtigung niedrig: die Auswertung ist read-only. Evidenz hoch: vermischte Reports erzeugen falsche Gate-Aussagen. / Permission low: reporting is read-only. Evidence high: mixed reports create false gate claims. | Zwei Cobertura-Sätze mit verschiedenen Raten; nur der benannte Lauf darf in die Summary eingehen. / Two Cobertura sets with different rates; only the named run may enter the summary. | `Promote` |
| AR-006 | Vollhistorischer Gitleaks-Fund in einer alten Inventar-CSV benötigt externe Credential-Disposition. / Full-history Gitleaks findings in an old inventory CSV require external credential disposition. | Feature 002, `G-SUPPLY-001`; `docs/security/secure-development/2026-08-30-secure-development-hardening/README.md`; nur lokaler Scanbefund, keine Credential-, Provider-, Widerrufs- oder Merge-Tatsache. / Feature 002, `G-SUPPLY-001`; tracked local scan finding only, with no credential, provider, revocation, or merge fact. | Deterministischer Security-Befund mit projektspezifischem Inhalt; die bestehende allgemeine Regel wird nicht dupliziert. / Deterministic security finding with project-specific content; the existing general rule is not duplicated. | `project-specific implementation` | Commit-ID, CSV-Inhalt und mögliche Schlüsselwerte dürfen nicht als portable Regel exportiert werden. / Commit ID, CSV content, and possible key values must not be exported as a portable rule. | Allgemein gilt bereits: historische Secret-Funde bleiben releaseblockierend bis Prüfung oder Widerruf; kein neuer Regeltext nötig. / The general rule already exists: historical secret findings remain release-blocking until review or revocation; no new rule text is needed. | 1 | High | Berechtigung kritisch: Credential-Prüfung oder Widerruf braucht externe Autorität. Evidenz hoch: lokale Erkennung beweist weder Gültigkeit noch Widerruf. / Permission critical: credential review or revocation requires external authority. Evidence high: local detection proves neither validity nor revocation. | Bestehende Provider-/Credential-Prüfung; keine Fixture mit realen Werten erzeugen. / Existing provider/credential review; create no fixture with real values. | `RejectProjectSpecific` |

Korrektheits-, Sicherheits-, Berechtigungs- und Evidenzintegritätsdefekte dürfen
nach einem deterministischen Vorkommen übernommen werden. Reine
Effizienzpräferenzen benötigen mindestens zwei unabhängige Feldbeobachtungen.

*Correctness, security, permission, and evidence-integrity defects may be
promoted after one deterministic occurrence. Pure efficiency preferences need
at least two independent field observations.*

## Outcome / Ergebnis

- Local non-empty correction / Lokale nicht leere Korrektur: Feature-002-Evidenz, Retrospektive und kandidatenbezogene Statistik; die validierte Lineage liegt recoverbar im lokalen Stash, bleibt aus dem PR-Kandidaten ausgeschlossen und wird erst nach T117 angewendet; keine Runtime-/Providerdatei wird geliefert. / Feature 002 evidence, retrospective, and candidate-only statistics; the validated lineage remains recoverable in the local stash, excluded from the PR candidate, and is applied only after T117; no runtime/provider file is delivered.
- Portable handoff / Portable Übergabe: AR-001 bis AR-005 in dieser Retrospektive; keine Änderung außerhalb des Repositorys autorisiert. / AR-001 through AR-005 in this retrospective; no change outside the repository is authorised.
- Pending observations / Offene Beobachtungen: exakter Provider-Axe-, Supply-Chain-, Drei-Plattform-, Review- und Credential-Nachweis in T108–T114. / exact provider Axe, supply-chain, three-platform, review, and credential evidence in T108–T114.
- Rejected project details / Verworfene Projektdetails: AR-006 bleibt ausschließlich in der lokalen Security-/Supply-Chain-Evidenz. / AR-006 stays only in local security/supply-chain evidence.
- Changed surfaces / Geänderte Oberflächen: Diese Phase präzisiert ausschließlich Klassifikation und Evidenzgrenzen in `specs/002-secure-development-hardening/retrospective.md`; keine Skill-, Runbook-, Template-, Skript-, Governance- oder Provideroberfläche wurde geändert. / This phase only clarifies classification and evidence boundaries in `specs/002-secure-development-hardening/retrospective.md`; no skill, runbook, template, script, governance, or provider surface was changed.
- Validation / Validierung: Alle sechs Beobachtungen bestehen den vollständigen Retrospektivvertrag, alle 13 akzeptierten Artefakthashes stimmen, der read-only Run-State-Validator besteht mit 107/122 Tasks, T001–T107 sind vollständig markiert, und T108–T122 bleiben vollständig offen; Remote-Fakten wurden nicht ergänzt. / All six observations pass the complete retrospective contract, all 13 accepted-artifact hashes match, the read-only run-state validator passes at 107/122 tasks, T001–T107 are fully checked, and T108–T122 remain fully open; no remote facts were added.
- Next field gate / Nächstes Feld-Gate: T108 richtet Version und beabsichtigte Staging-Menge aus; T111–T114 konvergieren Provider-/Review-/PreMerge-Evidenz. / T108 aligns version and intended staging; T111–T114 converge provider/review/PreMerge evidence.
- Resume-state quality / Qualität des Fortsetzungsstatus: `Valid`; `autonomous-run-state.json` blieb coordinator-owned und lieferte die exakte T095-Fortsetzungsaktion. / `Valid`; the state remained coordinator-owned and supplied the exact T095 resume action.
